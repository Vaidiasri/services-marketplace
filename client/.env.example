# Copy to .env.local. Only VITE_-prefixed values reach the browser bundle,
# so nothing secret may ever live here.

VITE_API_URL="http://localhost:3000"

# Phase 7 (M7): shows the mock payment token selector in the booking flow so a
# reviewer can trigger tok_fail from the UI. Deliberately on for the deployed demo.
# VITE_SHOW_PAYMENT_TOKENS="true"
